[project]
name = "zivoc_branding"
version = "0.0.1"
description = "Zivoc UI branding overrides for ERPNext"
requires-python = ">=3.10"
dependencies = []

[build-system]
requires = ["flit_core>=3.4,<4"]
build-backend = "flit_core.buildapi"

[tool.bench.dev-dependencies]
